# Legacy Task Samples

这个目录只存放旧的 `npm run task:legacy -- --taskFile ...` 样例。

默认不要使用这些文件。默认请改用：

- `..\doubao-job.example.json`
- `..\publish-from-spu.job.example.json`

保留这些旧文件的目的只有两个：

- 兼容老脚本
- 方便回溯旧任务结构
